{
  "name": "kabukicho-project",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "vite frontend",
    "electron": "electron .",
    "app": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && npm run electron\""
  },
  "devDependencies": {
    "electron": "^latest",
    "concurrently": "^latest",
    "wait-on": "^latest",
    "vite": "^latest"
  }
}